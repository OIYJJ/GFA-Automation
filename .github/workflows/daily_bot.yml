name: GFA Marketing Bot (Lee's PC)

on:
  schedule:
    - cron: '30 20 * * *' # 05:30 KST
    - cron: '0 4 * * *'   # 13:00 KST
    - cron: '20 6 * * *'  # 15:20 KST
  workflow_dispatch:
    inputs:
      force_main_run:
        description: "true 입력 시 즉시 데이터 수집 실행"
        required: false
        default: "true"

concurrency:
  group: gfa-marketing-bot-lee-pc
  cancel-in-progress: true

env:
  WORK_DIR: "C:\\Users\\82108\\Desktop\\업무자동화"
  PROFILE_PATH: "C:\\chrometemp"

jobs:
  run-local-marketing-bot:
    runs-on: [self-hosted, windows]
    timeout-minutes: 20

    steps:
      - name: 1) 시간대 및 실행 모드 설정
        shell: cmd
        run: |
          chcp 65001
          set "FORCE_MAIN=${{ github.event.inputs.force_main_run }}"
          set "SCHEDULE=${{ github.event.schedule }}"

          if /I "%FORCE_MAIN%"=="true" (
            echo 🌙 [수동-강제] MAIN_RUN=true
            echo MAIN_RUN=true>> "%GITHUB_ENV%"
          ) else if "%SCHEDULE%"=="30 20 * * *" (
            echo 🌙 [새벽 모드] 메인 수집 + 업로드 활성화
            echo MAIN_RUN=true>> "%GITHUB_ENV%"
          ) else (
            echo ☀️ [업무 모드] 세션 연장 모드
            echo MAIN_RUN=false>> "%GITHUB_ENV%"
          )

      - name: 2) 사전 청소 및 복구 팝업 원천 차단
        shell: cmd
        run: |
          chcp 65001
          echo 🧹 파이썬 실행 전, 엉켜있는 크롬 찌꺼기를 정리합니다...
          powershell -NoProfile -Command "Get-CimInstance Win32_Process | Where-Object { $_.Name -match 'chrome(driver)?\.exe' -and $_.CommandLine -like '*chrometemp*' } | Stop-Process -Force -ErrorAction SilentlyContinue"
          
          if exist "${{ env.PROFILE_PATH }}\SingletonLock" del /f /q "${{ env.PROFILE_PATH }}\SingletonLock"
          
          echo 🛠️ 크롬 '비정상 종료' 팝업을 띄우지 못하도록 기록을 세탁합니다...
          set "PREF_FILE=${{ env.PROFILE_PATH }}\Default\Preferences"
          if exist "%PREF_FILE%" (
            powershell -NoProfile -Command "$p='%PREF_FILE%'; $t=Get-Content $p -Raw; if($t -match 'exit_type\":\"Crashed'){ $t2=$t -replace 'exit_type\":\"Crashed','exit_type\":\"Normal'; Set-Content $p $t2 -Encoding UTF8; echo '✅ 팝업 방지 패치 완료' }"
          )

      - name: 3) GFA 데이터 수집 (파이썬이 직접 크롬 실행)
        shell: cmd
        working-directory: ${{ env.WORK_DIR }}
        run: |
          chcp 65001
          set "PYTHONIOENCODING=utf-8"
          python gfa_bot_final.py

      - name: 4) 구글 시트 업로드
        if: env.MAIN_RUN == 'true'
        shell: cmd
        working-directory: ${{ env.WORK_DIR }}
        run: |
          chcp 65001
          set "PYTHONIOENCODING=utf-8"
          python gfa_to_sheet.py
