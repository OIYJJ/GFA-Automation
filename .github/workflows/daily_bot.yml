name: GFA Marketing Bot (Lee's PC)

on:
  # 1. 스케줄 실행: 매일 한국 시간 05:30 (UTC 20:30)
  schedule:
    - cron: '30 20 * * *'
  
  # 2. 수동 실행: 테스트용 버튼 유지
  workflow_dispatch:

jobs:
  run-local-marketing-bot:
    runs-on: self-hosted
    
    steps:
      - name: 1. 자동화 시작 & 한글 설정
        shell: cmd
        run: |
          chcp 65001
          echo "🚀 [Auto] 05:30 정기 실행: GFA 마케팅 봇을 가동합니다..."

      - name: 2. 크롬 정리 (로그인 정보 유지)
        shell: cmd
        run: |
          echo "🧹 실행 중인 크롬 프로세스만 종료합니다 (쿠키 보존)..."
          taskkill /F /IM chrome.exe /T 2>nul
          
          echo "✅ 프로세스 정리 완료. 3초 대기..."
          ping 127.0.0.1 -n 4 > nul

      - name: 3. 크롬 실행 및 데이터 수집
        shell: cmd
        run: |
          echo "🌐 크롬 브라우저를 엽니다 (로그인 정보 로드)..."
          start "" "C:\Program Files\Google\Chrome\Application\chrome.exe" --remote-debugging-port=9222 --user-data-dir="C:\chrometemp" --no-sandbox --disable-gpu --disable-dev-shm-usage
          
          echo "⏳ 로그인 세션 로딩 및 페이지 안정화 (10초 대기)..."
          ping 127.0.0.1 -n 11 > nul
          
          echo "🔍 파이썬 수집 스크립트 실행..."
          set PYTHONIOENCODING=utf-8
          cd /d "C:\Users\82108\Desktop\업무자동화"
          python gfa_bot_final.py

      - name: 4. 구글 시트 업로드
        shell: cmd
        run: |
          echo "☁️ 구글 시트 업로드 실행..."
          set PYTHONIOENCODING=utf-8
          cd /d "C:\Users\82108\Desktop\업무자동화"
          python gfa_to_sheet.py

      - name: 5. 작업 완료 및 PC 종료
        shell: cmd
        run: |
          echo "✅ 모든 작업이 완료되었습니다."
          echo "60초 뒤 PC가 종료됩니다. (취소하려면 Win+R -> shutdown -a)"
          shutdown -s -f -t 60
